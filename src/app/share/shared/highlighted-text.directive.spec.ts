import { HighlightedTextDirective } from '../highlighted-text.directive';

describe('HighlightedTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightedTextDirective();
    expect(directive).toBeTruthy();
  });
});
